# ArchiveEntry

Package `org.apollo.cache.archive`.

Defined in [`2006Scape Server/src/main/java/org/apollo/cache/archive/ArchiveEntry.java`](2006Scape Server/src/main/java/org/apollo/cache/archive/ArchiveEntry.java).

Represents a single entry in an {@link Archive}.  @author Graham

```java
public final class ArchiveEntry {
public ArchiveEntry(int identifier, ByteBuffer buffer)
public ByteBuffer getBuffer()
public int getIdentifier()
```
