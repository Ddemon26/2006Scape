# Parabot Overview

Parabot is a scripting client that allows automated interaction with 2006Scape. After downloading the latest release from GitHub start it with:

```bash
java -jar Parabot.jar -local
```

Scripts can then connect to your local server for testing.
