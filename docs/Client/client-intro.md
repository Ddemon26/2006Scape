# Client Overview

The client is a modified RuneScape 289 revision written in Java. It communicates with the server using the classic protocol and can run locally for development or be packaged with the launcher for players.

When running the client locally ensure the server is active on `localhost` so it can connect correctly.
