# EntityMovementHandler

Defined in [`2006Scape Client/src/main/java/core/EntityMovementHandler.java`](../../2006Scape%20Client/src/main/java/core/EntityMovementHandler.java).

Updates entity movement logic extracted from [`Game`](Game.md).

```java
final class EntityMovementHandler {
    EntityMovementHandler(Game game)
    void updateEntityMovement(Entity entity)
    void updateForcedMovement(Entity entity)
    void updateInterpolatedMovement(Entity entity)
    void updateWalkingStep(Entity entity)
}
```
