# FlamesEffect

Defined in [`2006Scape Client/src/main/java/core/FlamesEffect.java`](../../2006Scape%20Client/src/main/java/core/FlamesEffect.java).

Manages the flame background effect shown on the login screen. Extracted from [`Game`](Game.md).

```java
final class FlamesEffect {
    FlamesEffect(Game game)
    void calcFlamesPosition()
    boolean saveWave(byte[] data, int i)
    void randomizeBackground(Background background)
    void doFlamesDrawing()
    void drawFlames()
}
```
